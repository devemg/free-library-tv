import { NavigationEndpointDirective } from './navigation-endpoint.directive';

describe('NavigationEndpointDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationEndpointDirective();
    expect(directive).toBeTruthy();
  });
});
